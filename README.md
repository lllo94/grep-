# grep+
a simple tool to find a certain word in files, written with c++


# install

```
cd grep- \
chmod +x install.sh
./install.sh
```

# usage 

```
grep+ -f file -q query
```
or
``` 
grep+ --file file --query query
```
